pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'pip install -r E2E_saucedemo_website/requirements.txt'
            }
        }
        stage('Run Tests') {
            steps {
                sh 'python E2E_saucedemo_website/test_saucedemo.py'
            }
        }
    }
}
